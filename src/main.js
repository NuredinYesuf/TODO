import { createApp } from 'vue'
import App from './App.vue'
import './main.css'
import hello from './hello.vue'

createApp(App).mount('#app')


// identify the main purpose of the folloin 
// gimort s that are a direct link eto the vue file
// the local server seems to change very tim ethe vite comand os promted 
 